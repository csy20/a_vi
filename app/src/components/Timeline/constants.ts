export const PIXELS_PER_FRAME  = 5   // px per frame at 1× zoom
export const TRACK_HEIGHT       = 48  // px — height of one track row
export const RULER_HEIGHT       = 28  // px — height of the frame ruler
export const TRACK_LABEL_WIDTH  = 160 // px — width of the label sidebar
export const CLIP_PADDING       = 5   // px — vertical padding inside a track row
